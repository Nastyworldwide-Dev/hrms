# `as-hr_kpi` → v16 Migration Plan

- **Last updated:** 2026-08-14 (Round 6 — PMS/Appraisal/KPI domain closed)
- **Working branch:** `nz-version-16` — implementation complete and **uncommitted**. Nothing staged, committed or pushed. See §10b for final dispositions and verification.
- **Evidence tags:** **[V]** verified from code/config · **[I]** inferred (strong, not executed) · **[U]** unknown / needs site or stakeholder input
- Companion: [`hrms-system-baseline.md`](./hrms-system-baseline.md)

> **Framing correction (Round 3).** Round 2 treated the donor as an optional behavioural reference and, by trusting the 2026-08-10 port series, examined only the commits made after it. `as-hr_kpi` is an **authoritative migration source**: every donor change is presumed required until evidence shows otherwise. Round 3 therefore re-derived the donor's true base and mechanically classified the **entire** delta — 312 commits, 441 files.
>
> **Arithmetic correction (Round 3.1).** The Round 3 completeness statement claimed 196 already-satisfied commits, giving 196+4+2+1 = 203 ≠ 205, and its capability table carried hand-estimated counts that summed to 214 across 29 rows while being labelled "26 clusters". **No commits were missing.** The dispositions were right; the counts were not. §2 is now generated by a deterministic, re-runnable classifier instead of hand tallies, and the corrected figure is **198** already satisfied (198+4+2+1 = 205 ✔).

---

## 1. Verified branch, commit, version, and ancestry

| Ref | Commit | Version | Relationship |
|---|---|---|---|
| `nz-version-16` (active) | `768792f6548bf9c1a28a9c0c4eec6099d753d55d` | 16.19.0 | **identical to `origin/version-16`** (`git rev-list --left-right --count` → `0 0`); published as `origin/nz-version-16` at the same commit |
| `origin/version-16` | `768792f65` | 16.19.0 | read-only baseline |
| `origin/version-15` | `dd6c388f71d38dd4b92cd0d19178a27f19acfab6` | 15.58.2 (2026-02-25) | **the donor's true base** |
| `origin/as-hr_kpi` (donor) | `2fb61f39952f0c0a8143f6f91f192ac662cbdce4` | 15.111.1 (2026-08-13) | direct descendant of `version-15` |

**[V] Donor base verified:** `git merge-base origin/version-15 origin/as-hr_kpi` = `dd6c388f7` = `origin/version-15` HEAD exactly. The donor is a linear, **merge-free** descendant, so the authoritative migration range is:

```
dd6c388f71d38dd4b92cd0d19178a27f19acfab6 .. 2fb61f39952f0c0a8143f6f91f192ac662cbdce4
```

```
frappe/hrms upstream
   └─ 1607f9a6a (2023-10-23) common ancestor of both lines
        ├─ version-15  dd6c388f (v15.58.2) ── 312 commits ──► as-hr_kpi 2fb61f399 (v15.111.1)
        │                                        │  (fork's complete custom layer)
        │        hand port, 11 commits 2026-08-10 ┘
        └─ version-16 (upstream v16 + ported layer + v16-only sync/fence work) ──► 768792f65 (v16.19.0)
                └─ nz-version-16 (== version-16)
```

**[V] The port was a hand port, not a merge** — no merge commit joins the lines, which is why an exhaustive file-level reconciliation (§3) was required rather than a `git log` comparison.

---

## 2. Complete donor commit inventory — all 312 classified

| Metric | Count |
|---|---|
| Total commits in range | **312** |
| `chore(release)` version-bump commits | **107** |
| Merge commits | 0 |
| **Substantive commits** | **205** |
| Files touched (net delta) | **441** (282 added · 152 modified · 7 deleted · 0 renamed) |

### 2.1 Substantive capabilities — 24 capabilities, 205 commits

Every substantive commit belongs to **exactly one** capability, and every capability carries **exactly one** disposition. File counts are files touched by that capability's commits and **overlap between capabilities** (a shared file such as `hooks.py` is touched by several), so they intentionally do not sum to 441 — §3 is the non-overlapping file reconciliation.

| ID | Capability | Commits | Files | Principal paths | Disposition | WP |
|---|---|---:|---:|---|---|---|
| C01 | NSTY PMS appraisal model — 70/20/10, A1/A2 split, B4/B5 evidence, demerits, leadership gate, score-conversion table, weightage redistribution, My KPI dashboard, appraisal visibility scope | 63 | 148 | `hrms/hr/doctype/appraisal*`, `frontend/src/views`, `frontend/src/components` | Already satisfied | — |
| C04 | Overtime — OT Request, Replacement Leave Claim, tiered OT bands, rate-weighted hours, `get_ot_pay` | 11 | 28 | `hrms/hr/doctype/{ot_request,overtime_*,attendance_overtime_band}` | Already satisfied | — |
| C05 | Geofenced check-in — strict/lenient modes, Remote Checkin Request, selfie capture, approval routing, stale-IN sweeper, reject log | 27 | 82 | `hrms/hr/doctype/{employee_checkin,remote_checkin_request}`, `hrms/utils`, `hrms/api/geofence.py` | Already satisfied | — |
| C06 | SOP library — department scope, HR authoring, PDF reader, S3 same-origin streaming, attachment lifecycle | 9 | 21 | `hrms/hr/doctype/sop_document`, `hrms/api/sop.py`, `frontend/src/views/sop` | Already satisfied | — |
| C07 | Employee Issue helpdesk — PWA tickets, HR board, hook hardening | 6 | 26 | `hrms/hr/doctype/employee_issue`, `frontend/src/views/issues` | Already satisfied | — |
| C08 | Permissions & staff lockdown — permission matrix, approver fence, server-clock punch, permlevel guards, HRMS-only UP scope, HR Contact directory, UP neutralisation on approval doctypes | 24 | 148 | `hrms/overrides/*_row_scope.py`, `hrms/utils/user_permission_scope.py`, `hrms/patches` | Already satisfied (v16 expanded) | — |
| C09 | Employee master — Notice Period Unit field | 1 | 3 | `hrms/patches` | Already satisfied | — |
| C10 | Shift rules & breaks — location/department rules, auto leave-policy assignment, shift swap, flexible break type, unpaid-break deduction | 7 | 33 | `hrms/hr/shift_rules.py`, `hrms/hr/doctype/{shift_swap_request,shift_location,shift_break}`, `hrms/utils/break_calculation.py` | Already satisfied (**v16 superset**) | — |
| C12 | Leave rules — service-entitlement slabs, grade slabs, pro-rating opt-out, years-of-service, balance cards, leave-details guards | 17 | 40 | `hrms/hr/doctype/{leave_type*,leave_policy_assignment,leave_control_panel}`, `hrms/hr/leave_rules.py` | Already satisfied | — |
| C16 | Attendance Request — in/out times, mandatory attachment, self-submission block | 4 | 6 | `hrms/hr/doctype/attendance_request` | Already satisfied | — |
| C17 | Regional payroll — Malaysia, UAE (GPSSA/WPS), China | 4 | 29 | `hrms/regional/*` | Already satisfied | — |
| C18 | Interco salary cost allocation — percentage split (Cost Tag added then dropped) | 3 | 18 | `hrms/hr/doctype/employee_interco_allocation`, `hrms/payroll` | Already satisfied | — |
| C19 | Employment Hero Settings — encrypted OAuth2/PKCE store | 1 | 5 | `hrms/hr/doctype/employment_hero_settings` | Already satisfied | — |
| C20 | Skills gap matrix report | 2 | 5 | `hrms/hr/report`, `hrms/hr/doctype/designation_skill` | Already satisfied | — |
| C21 | PWA UX — Modernist redesign, tablet/desktop, dark mode, theme toggle, tab bar, one-tap approve | 12 | 28 | `frontend/src/{components,views,data}` | Already satisfied | — |
| C23 | PWA notifications — auto-prompt, escalation, scoping, message backfill, inline approve/reject | 6 | 13 | `hrms/hr/doctype/pwa_notification`, `frontend/src/views` | Already satisfied | — |
| C26 | Tooling & docs — lint debt, service-band doc note | 1 | 1 | `hrms/hr` | Already satisfied | — |
| **G1** | System Manager excluded from team-activity see-all (`HR_SEE_ALL_ROLES`) | 1 | 6 | `hrms/hr/utils.py`, `hrms/overrides/{approval,ot}_row_scope.py` | **Forward-port** | WP-5 |
| **G2** | Managers read direct reports' requests (read-only, status-filtered) | 1 | 6 | `hrms/overrides/approval_row_scope.py`, `hrms/tests` | **Forward-port** | WP-6 |
| **G3** | Team day view | 1 | 10 | `hrms/api/team.py`, `hrms/utils/team_status.py`, `frontend/src/views/team` | **Reimplement (v16-native)** | WP-7 |
| **G4** | Approvals history tab + always-visible Replacement Leave card | 1 | 8 | `hrms/api/__init__.py`, `frontend/src/{data,components}` | **Forward-port** | WP-8 |
| **G5** | OT claim grace window to day 7 of following month | 1 | 4 | `hrms/utils/filing_window.py`, `hrms/hr/doctype/ot_request` | **Blocked — HR/payroll decision** | WP-9 |
| **G6** | `description` on 6 Appraisal child tables | 1 | 8 | `hrms/hr/doctype/appraisal_*`, `employee_feedback_rating` | **Forward-port** | WP-8 |
| **G7** | PWA time-field picker | 1 | 2 | `frontend/src/components/FormField.vue` | **Reimplement (v16-native)** | WP-8 |
| | **Total substantive** | **205** | | | | |

**Disposition totals:** already satisfied **198** · forward-port **4** (G1, G2, G4, G6) · reimplement **2** (G3, G7) · blocked **1** (G5). **198 + 4 + 2 + 1 = 205 ✔**

### 2.2 Release-only commits — 107

**[V]** All 107 `chore(release)` commits bump `hrms/__init__.py`. **106 touch that file and nothing else.** The exception is **`2c73c1867` (v15.60.0)**, which also carried `.github/workflows/initiate_release.yml`, `.gitignore` and `.releaserc`.

Disposition — **already satisfied / superseded**, no migration item:
- `.gitignore` and `.releaserc` donor lines are at **100 % coverage** in v16 (§3.2).
- The workflow change was `version: ["14"] → ["15"]`; **v16 already carries `version: ["16"]`** (`.github/workflows/initiate_release.yml:18`), which is the correct value for this line. Porting the donor's `"15"` would be a regression.
- Version bumps themselves are release automation; v16 runs its own semantic-release on `version-16` (`.releaserc`), so donor version numbers must **not** be carried across.

### 2.3 How the classification is produced (reproducible)

Each commit is assigned by the **first** matching rule, so assignment is deterministic and total:

1. `chore(release)` in the subject → **R00 release-only** (107).
2. Explicit SHA in the gap set → **G1…G7** (7).
3. Subject matches the permission-layer vocabulary (`lockdown|user permission|permlevel|row scope|System Manager|neutralize|HRMS-only|HR Contact|restrict_user_permission|…`) **or** touches a permission-layer file → **C08**.
4. Feature rules in order: regional → SOP → Employee Issue → overtime → geofence/check-in → PMS/appraisal → shift/breaks → leave → attendance request → interco → Employment Hero → skills gap → PWA notifications → employee master → PWA UX → reports → tooling.
5. Anything unmatched falls back to **C08** (one commit did: `1c875dbbd`, a User-Permission handler timing fix — correctly a permissions change).

**Honest limitation [V]:** for cross-cutting commits the *choice* of capability follows this precedence, not semantic dominance — e.g. `f22c5a81d` ("SOP library") also changes permission code and lands in C06 by rule order. Because every such commit carries the **same disposition (already satisfied)**, precedence affects only which row a commit is counted in, never a total, a disposition, or a work package. The seven gap commits are pinned by SHA, so they cannot be absorbed by any rule.

### 2.4 Historical `apps/nsty` paths — not a migration item **[V]**
Early fork work lived in a separate `nsty` app; `f13e3f59c` migrated it into `hrms`. Per-commit file lists therefore show `apps/nsty/...` paths, but the **net delta contains zero `apps/nsty` paths and the donor's final tree contains none**. The §3 reconciliation is over the net delta, so nothing under `apps/nsty` is missed.

---

## 3. Mechanical reconciliation — how "already satisfied" was proven

Rather than trusting the port series, every file in the donor delta was compared against v16.

### 3.1 Added files (282) — **[V]**

| Result | Count |
|---|---|
| Present in v16, **byte-identical** to donor | **247** |
| Present in v16, **differs** (v16 adaptation) | 21 |
| **Absent from v16** | **14** |

**[V] All 14 absent files were traced to their introducing commit** with `git log --diff-filter=A`, and every one lands in the gap set: G3 ×7, G4 ×2, G5 ×2, G2 ×1, G6 ×1, G7 ×1. **No eighth commit contributes a missing file.** (G1 adds no new file; it modifies existing ones.)

Of the 21 that differ, **17 are v16 *expansions*** of donor work — e.g. `overrides/employee_hrms_scope.py` 130→210 lines, `overrides/sop_document_row_scope.py` 98→141 (company scoping), `tests/test_doctype_permission_integrity.py` 60→100, `api/remote_checkin.py` 422→460. **Four shrank, and every shrinkage traces to a known gap**: `overrides/approval_row_scope.py` −23 (G1+G2), `hr/doctype/ot_request/test_ot_request.py` −27 (G5 tests), `frontend/src/views/More.vue` −9 (G3 nav entry), and four appraisal child JSONs −6 each (G6).

### 3.2 Modified files (152) — **[V]**

For each file, the lines the donor **added** relative to its base were checked for presence in v16 (149 files carried meaningful additions):

| Donor-line coverage in v16 | Files |
|---|---|
| 100 % | **119** |
| 95–99 % | 5 |
| 80–94 % | 6 |
| < 80 % | 19 |

All 30 sub-100 % files were opened and triaged line by line. **[V] No unexplained donor addition remains** — each missing line is a gap commit (G1, G4, G6, G7), a v16 refactor of equivalent behaviour, a JSON key-order artefact, or a donor change v16 deliberately superseded (§4.4).

### 3.3 Deleted files (7) — **[V]**
All seven — the PWA salary-slip viewer (`SalaryDetailTable.vue`, `SalarySlipItem.vue`, `SemicircleChart.vue`, `SalaryIcon.vue`, `router/salary_slips.js`, `views/salary_slip/{Dashboard,Detail}.vue`) — are **also absent from v16**. The donor's security-motivated removal was honoured; `tests/test_staff_lockdown.py:121` pins it.

### 3.4 Inverse check — **[V]**
Donor *removals* still present in v16 were scanned (34 files). All are explained by upstream markup legitimately retaining similar lines, or by the same gap commits (`RequestPanel.vue` 7/7, `api/__init__.py` `approver_field_map`). One alarming hit — `appraisal.py::set_appraisal_template` — was disproved: the method is **byte-identical in both trees at line 483**; the donor never removed it.

---

## 4. The migration gap — 7 commits, 14 files

| ID | Donor commit | Capability | Donor artefacts | v16 target | Disposition |
|---|---|---|---|---|---|
| **G1** | `87ae095e7` | System Manager excluded from team-activity see-all | `hrms/hr/utils.py:54` `HR_SEE_ALL_ROLES`; `overrides/approval_row_scope.py:42`; `overrides/ot_row_scope.py:23` | same paths (v16 `hr/utils.py:51` currently `HR_ROLES = {HR User, HR Manager, System Manager}`) | **Forward-port, adapted** |
| **G2** | `acad140f8` | Managers read direct reports' requests (read-only, status-filtered both sides) | `overrides/approval_row_scope.py` `_report_employees()`; `tests/test_manager_leave_visibility.py` | `hrms/overrides/approval_row_scope.py` | **Forward-port + company-fence composition** |
| **G3** | `dbc63acd0` | Team day view | `hrms/api/team.py`, `hrms/utils/{team_status,test_team_status}.py`, `frontend/src/{data/team.js,views/team/TeamDashboard.vue,components/icons/TeamIcon.vue}`, `tests/test_team_api.py`, `views/More.vue`, `router/index.js` | new files, same paths | **Port with redesign** (every read is `ignore_permissions=True`) |
| **G4** | `1cca17e28` | Approvals history tab + always-visible Replacement Leave card | `api/__init__.py` (`history` param, `APPROVER_FIELD_MAP`, RL balance via `get_leave_balance_on`), `frontend/src/data/{leaves,claims,attendance}.js`, `components/{RequestPanel,ReplacementLeaveCard}.vue`, `views/leave/Dashboard.vue`, `tests/test_requests_history_api.py` | same paths | **Forward-port** |
| **G5** | `4e33cad0d` | OT claim grace window to day 7 of following month | `hrms/utils/{filing_window,test_filing_window}.py`, `hr/doctype/ot_request/ot_request.py`, `test_ot_request.py` | v16 `ot_request.py:38-52` rejects all out-of-month filing | **Blocked — HR/payroll decision** |
| **G6** | `9a52eaa44` | `description` on 6 Appraisal child tables | `appraisal_kra`, `appraisal_goal`, `appraisal_functional_competency`, `appraisal_b4_evidence`, `appraisal_b5_evidence`, `employee_feedback_rating` JSONs; `tests/test_appraisal_child_descriptions.py` | same JSONs | **Forward-port (schema-only)** — reconcile with `.claude/plans/appraisal-achievement-scoring.md`, which edits the same files |
| **G7** | `2614bd5ae` | Time fields render a real picker | `frontend/src/components/FormField.vue` native-input branch; `frontend/tests/formfield-time-input.test.mjs` | same file — **v16 diverged** (added a `TextEditor` branch the donor lacks) | **Reimplement on v16's file** |

### 4.4 Donor changes deliberately **not** ported (superseded — evidence-backed)

These sit inside the 198 already-satisfied commits; the *capability* is present, the donor's *implementation* was replaced by a better one.

| Donor change | Why superseded | Evidence |
|---|---|---|
| `docstatus = 1` filters + `.format()` condition builders in `salary_payments_via_ecs`, `provident_fund_deductions`, `salary_payments_based_on_payment_mode` | v16 rewrote all three in the query builder **keeping** the docstatus restriction and replacing string interpolation with pypika binding — **v16 is strictly safer** | v16 `salary_payments_via_ecs.py:111`, `provident_fund_deductions.py:126`, `salary_payments_based_on_payment_mode.py:51,78` vs donor `.format()` at `provident_fund_deductions.py:62-77` |
| `get_payroll_entries_for_jv` raw-SQL parameterisation | v16 rewrote as query builder with `@frappe.validate_and_sanitize_search_inputs` + `docstatus == 1` | v16 `payroll_entry.py:1703-1726` |
| `vehicle_expenses` `get_period_dates` | v16 uses the same helper more cleanly **and** adds a docstatus filter the donor lacks in `get_service_expense` | v16 `vehicle_expenses.py:64,86,124` |
| Employee `cancel` granted in **JSON** on Leave Application / Expense Claim / Shift Request | Donor rows granted `cancel` **without `submit`**, which aborts fresh v16 installs; v16 removed them and grants cancel **at runtime** | v16 `patches/v15_106_3/allow_staff_cancel_own_requests.py:42`; rationale in `tests/test_doctype_permission_integrity.py` docstring |
| `Upload Attendance` changes, `translations/ms.csv` | Doctype deprecated in v16 (`41200400a` + `patches/v16_0/delete_upload_attendance_doctype`); translations moved to `hrms/locale` | v16 `patches.txt` |
| Release workflow `version: ["15"]` (`2c73c1867`) | v16 correctly targets `version: ["16"]` | `.github/workflows/initiate_release.yml:18` |

---

## 5. Migration order and dependencies

```
WP-0 site verification ──┬─► WP-1 leave draft (v16 defect)
                         ├─► WP-2 fence guard test ─► WP-3 close RC-A ─► WP-4 close RC-B
                         │                                     │
                         │                                     ▼
                         └───────────────────────────► WP-5 G1 ─► WP-6 G2 ─► WP-7 G3
                                                                  │
WP-8 G4 · G6 · G7 (independent, after WP-1) ◄─────────────────────┘
WP-9 G5 (needs HR decision)      WP-10 ERP role authority (needs ERP design)
```

**Ordering rationale — [V]:** G2 widens who may *read* team requests and G3 reads with `ignore_permissions`, so both must land **after** the see-all set is corrected (G1) and after the doctype/report fences are closed (WP-3/WP-4). Shipping G2 or G3 first would widen access on top of an already-open surface.

| WP | Donor evidence (reverse traceability) | Target files |
|---|---|---|
| WP-0 | — (validates §6 v16 defects) | none |
| WP-1 | — (v16-only regression, §6.1) | `leave_application.py`, `hr/utils.py`, `api/__init__.py`, new test |
| WP-2 | — (v16 defect guard) | `tests/test_employee_role_fence_integrity.py` |
| WP-3 | — (v16 defect, §6.2) | doctype JSONs + row-scope hooks + runtime patch |
| WP-4 | — (v16 defect, §6.2) | report JSONs + patch |
| WP-5 | **G1** `87ae095e7` | `hr/utils.py`, `approval_row_scope.py`, `ot_row_scope.py` |
| WP-6 | **G2** `acad140f8` | `approval_row_scope.py`, `tests/test_manager_leave_visibility.py` |
| WP-7 | **G3** `dbc63acd0` | `api/team.py`, `utils/team_status.py`, `frontend/src/views/team/*` |
| WP-8 | **G4** `1cca17e28`, **G6** `9a52eaa44`, **G7** `2614bd5ae` | per §4 |
| WP-9 | **G5** `4e33cad0d` | `utils/filing_window.py`, `ot_request.py` |
| WP-10 | — (symptoms 4–6) | new module |

---

## 6. Independent v16 defects — *not* donor migration items

### 6.1 Leave draft blocked (Symptom 1) — **[V] v16-only upstream cherry-pick**
The only behavioural addition to `LeaveApplication.validate()` between donor and v16 is `c7fb62c05` (2026-08-05), whose `validate_leave_approver` (`leave_application.py:898-904`) throws *"Leave Approver is mandatory"* on **draft** saves (`docstatus != 2`), gated by an HR Setting shipping `default: "1"`. The donor has no such check. Ruled out with evidence: write-block (`Leave Application` is not mirrored), workflow (none ships), permlevel-1 `status` (re-seeded from `frappe.new_doc()`), and `validate_staff_approver` (**byte-identical** in both trees). Secondary, present in both trees: `get_department_approvers()` offers the department **ancestor chain** while `validate_staff_approver` accepts only the employee's **own** department.

### 6.2 Authorization surface (Symptom 2) — **[V] mechanism, [I] reachability**
- **RC-A** ~20 sensitive doctypes grant `Employee` permlevel-0 rights with no row-scope hook and no `if_owner`, fenced only by a User Permission that stops applying when absent or `applicable_for`-scoped (corroborated by `overrides/company_scope.py:11-15`).
- **RC-B** all 12 reports carrying the `Employee` role are **Script Reports** with raw SQL and zero permission checks (`shift_attendance` 8 queries / 0 checks), gated only by a doctype-level `has_permission(ref_doctype, "report")`.
- **RC-C** `HR_ROLES` includes `System Manager`, granting HR-level read scope across 5 surfaces — the gap **G1** closes.

### 6.3 v16 is *less* restrictive than both v15 and the donor **[V]**

| Item | v15.58.2 base | Donor | v16 | Assessment |
|---|---|---|---|---|
| `Salary Structure Assignment` → `Employee` role | **no row** | **no row** | **`read` + `select`** | **v16 introduced org-wide read of every employee's salary structure assignment.** The donor never touched this file — inherited from upstream v16, not a port drop. Sharpest instance of RC-A; verify first in WP-3. |
| `Leave Application` → `Leave Approver` `amend` | 1 | 1 | **0** | v16 removed approver `amend`. Confirm intentional. |
| `applicable_after` semantics | **working days** | working days | **calendar days** | Upstream v16 changed rule *and* field label coherently. **Not fork work** — `git log -S` confirms the working-days text predates the donor's base. Business must confirm which rule applies. |

---

## 7. Risk register

| ID | Sev | Finding |
|---|---|---|
| **R-1** | **P0 if WP-0 confirms no effective `allow=Employee` UP; else P1** | RC-A: ~20 sensitive doctypes readable/exportable org-wide by any Employee-role user — **aggravated** by §6.3 (v16 *added* the Salary Structure Assignment grant) |
| **R-2** | **P0/P1 (same trigger)** | RC-B: 12 Script Reports apply no row filtering |
| **R-3** | P1 | Leave Ledger dual-writer: hub-side approval writes an **unstamped** ledger row the write-block allows and `parity.py` cannot see |
| **R-4** | P1 | Symptom 1 blocks the most-used ESS workflow; untested in both trees |
| **R-5** | P2 | System Manager sees all teams' requests — fix = **G1** |
| **R-6** | P2 | Fail-open company fence |
| **R-7** | P2 | Approver-set mismatch (UI offers ancestors, backend rejects) |
| **R-8** | P3 | `check_geofence` has no ownership check on `employee` |
| **R-9** | P3 | Donor/v16 divergence is hand-ported: 7 commits / 14 files today, growing daily |
| **R-10** | P3 | Approver `amend` silently dropped in v16 (§6.3) |

---

## 8. Acceptance criteria and verification gates

**Per work package**
- **WP-1:** an employee with no configured approver saves a draft; submit behaviour matches the agreed policy; a UI-offered ancestor-department approver is accepted; existing approver tests stay green.
- **WP-2/3:** the new integrity test is red before and green after; a synthetic employee gets `PermissionError`/empty list on `/api/resource/<DocType>` **and** CSV export for a colleague's row; Salary Structure Assignment specifically returns nothing for a colleague; HR unaffected.
- **WP-4:** a synthetic employee running `Shift Attendance` and `Employee Analytics` sees only permitted rows or is refused; HR output unchanged.
- **WP-5 (G1):** a System-Manager-only user gets scoped query conditions on all five approval/OT doctypes; adding `HR User` restores see-all; `HR_ROLES` write-side fences unchanged.
- **WP-6 (G2):** manager reads a report's request; cannot write, submit, cancel, delete, share or amend it; an offboarded manager (inactive Employee, active User) sees nothing; no cross-company read.
- **WP-7 (G3):** HR-only manager selector; non-HR `manager` override raises `PermissionError`; users without reports get an empty payload; company fence respected on every `ignore_permissions` read.
- **WP-8:** G4 history returns only rows the caller could already see; G6 fields persist on all 6 child tables; G7 `type="time"` renders a picker with no regression to other fieldtypes.

**Gates (all pre-existing, all mandatory)**
`pre-commit` (ruff + prettier) → `bench --site <site> run-parallel-tests --app hrms --lightmode` → `tests/test_write_block.py` (AST wiring checks — **required for any `hooks.py` edit**) → `tests/test_doctype_permission_integrity.py` (**required for any doctype-JSON permission edit**: `cancel`/`amend` need `submit`; every permlevel>0 row needs a level-0 row — precisely what the donor's JSON `cancel` rows violated) → `patch.yml` CI (v14→v16 migration) for any new patch → commitlint / semantic-release conventions.

**Rollback:** one revertible commit per WP; permission changes ship as idempotent, re-runnable patches recording prior state; no destructive schema operations; branch re-verified as `nz-version-16` immediately before any commit or push.

---

## 9. Unresolved decisions and blockers

1. **G5 / WP-9 (HR + payroll):** is the day-7 grace window the agreed OT filing cutoff?
2. **§6.3 `applicable_after` (HR):** working days (v15/donor) or calendar days (v16)? Reverting means re-labelling the Leave Type field too.
3. **WP-1 (product/HR):** may a draft be saved without an approver? Who is the fallback when an employee has no `leave_approver`, no `reports_to`, and no department approver?
4. **WP-3 (product/security):** for each §6.1 doctype — own-record access, HR-only, or none? Especially `Salary Structure Assignment`.
5. **WP-7 (product):** should HR be able to browse any manager's team?
6. **WP-10 (ERP):** what is ERP-authoritative? Is there a feed, what freshness, fail-open or fail-closed when stale? **[V] No ERP→HRMS role or scope sync exists in either tree.**
7. **§6.3 (engineering):** was removing `Leave Approver.amend` intentional?
8. **G6 (engineering):** sequence against `.claude/plans/appraisal-achievement-scoring.md`, which edits the same child-table JSONs.
9. **Environmental:** WP-0 needs a staging site with synthetic data. No bench, no `frappe` module, no local site exists here (`localhost:8000` refused; the only live Frappe endpoint is production and was deliberately not used). Until WP-0 runs, R-1/R-2 severity stays a range.

---

## 10. Completeness statement

**[V] No donor change is unclassified, and no change is double-counted.**

| Axis | Reconciliation |
|---|---|
| **Commits** | 312 = 107 release-only + 205 substantive. Every substantive commit is assigned to exactly one of 24 capabilities by the deterministic rule order in §2.3; the classifier reports 0 unassigned. |
| **Dispositions** | 198 already satisfied + 4 forward-port + 2 reimplement + 1 blocked = **205 ✔** |
| **Capabilities** | 24 substantive (17 already-satisfied + 7 gap) + 1 release-only bucket = 25 buckets |
| **Files** | 441 net = 282 added + 152 modified + 7 deleted. Added: 247 byte-identical · 21 adapted (17 v16 expansions, 4 shrinkages each traced to G1/G2/G5/G3/G6) · 14 absent, each traced by `--diff-filter=A` to one of G2–G7. Modified: 149 with donor additions, 119 at 100 % coverage, 30 individually triaged. Deleted: 7, all absent from v16. |
| **Historical paths** | 0 `apps/nsty` paths survive in the net delta or the donor's final tree (§2.4). |

**Reverse traceability holds both ways:** every donor commit → capability → disposition (§2.1, appendix §11); every work package → its donor commits or an explicit v16-native defect label (§5).

---

## 10b. Implementation status — FINAL (2026-08-13)

Branch `nz-version-16`, **uncommitted**. Verification legend: **[RT]** executed on a real Frappe v16 site · **[S]** static/executable-here · **[W]** written only.

### Runtime environment built for verification

A disposable bench was created **on this machine** (no production, synthetic data only). Three blockers had to be cleared, each recorded because they gate anyone reproducing this:

| Blocker | Resolution |
|---|---|
| frappe v16.31.0 requires `Python >=3.14,<3.15`; box had 3.12.3 | `uv python install 3.14` (no root needed) |
| frappe v16 requires `node >=24`; box had 22.22.2 | local Node 24.9.0 tarball under `~/.local` |
| erpnext v16 test fixtures need the `payments` app | `bench get-app --branch version-16 payments` |

Bench at `/home/nabil/verify-bench`, sites `test.local` (upgrade-shaped) and `fresh.local` (clean install). **`apps/hrms` is a symlink to this working tree**, so the tests exercise the uncommitted code. Note `hrms/pyproject.toml` declares `requires-python = ">=3.10"` while its required frappe demands `>=3.14` — an inconsistency worth correcting separately.

### Donor capabilities — all seven have a final disposition

| ID | Capability | Disposition | v16 implementation | Verification |
|---|---|---|---|---|
| **G1** | System Manager excluded from team-activity see-all | **PORTED (adapted)** | `hr/utils.HR_SEE_ALL_ROLES`; used by `approval_row_scope`, `ot_row_scope`, `api/team.py` **and** the new `employee_owned_row_scope` — pay/PIP data is exactly the confidential access a technical role must not inherit | **[RT]** `test_manager_leave_visibility` 5/5, `test_employee_owned_row_scope::test_system_manager_is_not_hr` |
| **G2** | Managers read direct reports' requests | **PORTED (adapted)** | `hr/utils.get_direct_report_employees()` — one shared definition, read-only, status-filtered both sides, **plus a company fence the donor lacked** | **[RT]** 5/5 incl. new `test_company_fenced_manager_loses_out_of_company_reports` |
| **G3** | Team Day View | **PORTED (v16-native)** | `api/team.py`, `utils/team_status.py`, `frontend/src/{data/team.js,views/team/TeamDashboard.vue,components/icons/TeamIcon.vue}`, route + `navItems` + `More.vue` gate | **[RT]** `test_team_api` 6/6, `test_team_status` 11/11 |
| **G4** | Approvals history + Replacement Leave card | **PORTED** | `APPROVER_FIELD_MAP` + `history` mode in `get_filters` threaded through 3 endpoints; RL `balance_days`; History tab; `ReplacementLeaveCard.vue` | **[RT]** `test_requests_history_api` 4/4 |
| **G5** | OT grace window to day 7 | **IMPLEMENTED** — intent clearly evidenced | `utils/filing_window.py` + `ot_request.validate_filing_window` | **[RT]** `test_filing_window` 7/7, `test_ot_request` 12/12 |
| **G6** | `description` on appraisal child tables | **PORTED** | 7 child-table JSONs, donor field + grid position | **[S]** JSON validity, install |
| **G7** | PWA time picker | **REIMPLEMENTED** | native `<input type="time">` on v16's diverged `FormField.vue` | **[S]** `formfield-time-input.test.mjs` passes |
| **G8** | Remove employee advance requests | **PORTED (adapted)** | 7 PWA files deleted, entry points stripped from `Home.vue` / `expense_claim/Dashboard.vue` / `ListView.vue` / `router/index.js`; `employee_advance.json` read-only; `patches/v15_112_0/lock_employee_advance_readonly.py` aligns Custom DocPerm rows | **[RT]** read-only proven for every role on the live site; `test_employee_advance_readonly` 1/1, `test_employee_advance` 17/17 |

**G5 residual (the one irreducible decision):** the *value* 7 rests solely on the donor's comment `# payroll cutoff day of the following month (inclusive) — confirmed with HR 2026-08-12`. There is no config field in either tree, so changing it is a code edit. Also carried over deliberately: the replacement-leave **bank** claim window is untouched, so leave-branch OT filed during grace funds a bank whose own window has already closed. **Owner: HR/payroll.**

### Round 5 — donor advanced to v15.112.0

**[V] `origin/as-hr_kpi` moved `2fb61f399` → `a4debd2b6`** with one substantive commit,
`ddbdd6235 feat(hrms): remove employee advance requests`. Company policy: staff may not
request an advance and the company does not issue them. The senior removed the capability
on **both** sides, explicitly rejecting a UI-only fix — *"the routes stay reachable by deep
link and the create API stays open"*.

`nz-version-16` was entirely un-migrated: all PWA files present and the `Employee` role
still held `create` + `write`. The donor's own bench-free test failed against our JSON,
which is what proved the gap rather than an assertion.

**Adaptations made (why this is a port, not a cherry-pick):**
- `router/index.js` — advance routes removed while keeping the `/team` route from G3.
- `employee_advance.json` — the donor's file could **not** be taken wholesale: upstream v16
  renamed `exchange_rate` → `base_paid_amount`, so copying it would have reverted a v16
  schema change. Only the permission rows were hardened; the v16 field set is intact.
- `patches.txt` — appended after the two `v16_0` patches.
- `ListView.vue`, `Home.vue`, `expense_claim/Dashboard.vue` were byte-identical to the
  donor's pre-commit state, so those were taken wholesale.

**Judgement call recorded:** `Employee Advance Summary` stays staff-visible and
self-scoped. The donor deliberately keeps `read`/`report`/`export` "so historical records
stay visible"; a report showing the caller their own advances is that same intent, and
making it HR-only would invent a stricter policy than the senior wrote.

**Layering:** the donor leaves `read` org-wide. `overrides/employee_owned_row_scope.py`
already narrows `Employee Advance` to own records + HR-in-company, so the combination is
strictly stronger than either change alone.

**New pre-existing failure recorded, not fixed:** `test_expense_claim::test_expense_approver_perms`
sets an employee as their own expense approver, which the fork's staff-lockdown fence has
forbidden since long before this work (the throw is unchanged diff context at the
pre-migration base `768792f65`, and the test was never touched here). Same class as the
appraisal suites: an upstream test versus fork policy. Deciding whether the self-approval
ban deserves an exemption is HR policy, not a test fix.

### Round 6 — PMS / Appraisal / KPI domain closure

**The scoring contract (authoritative, read out of the code and identical in the donor):**

```
achievement    = actual / target * 100          (0 when target is 0)
capped         = clamp(achievement, 0, 100)     (overachievement earns no bonus)
rating_value   = capped / 100 * 5
weighted_score = per_weightage * capped / 100
weighted_avg   = weighted_sum / total_weightage * 5
conversion     = band lookup (cycle table, else DEFAULT_SCORE_CONVERSION)
a1_score       = min(conversion / 0.80 * a1_weight, a1_weight)
section_a      = a1_score + a2_score
pms_total      = clamp(section_a + section_b - demerits, 0, 100), with misconduct/leadership caps
final_score    = pms_total_score
```

KRA rows are copied from the template verbatim and **sum to 100**; the Section-A
weight is applied at scoring time, never baked into the row weights.
Bands are inclusive lower bounds and there is no 0% row, so an all-zero
achievement still lands on the floor band (0.50 → a1 43.75 at a1_weight 70).

**`goal_completion` / `goal_score` lifecycle — resolved: they are intentionally
dead.** `Appraisal.set_goal_score` is an explicit no-op — *"Legacy method — goal-based
scoring no longer used. Kept for API compatibility."* — and is **byte-identical in
`nz-version-16` and in the `as-hr_kpi` donor**, so this is the senior's deliberate
retirement, not a port regression. The columns survive so historical rows keep
rendering, and `hrms/api/kpi.py` reads them only as a fallback *behind* `achievement`.
Nothing should write them; `test_goal_scoring_is_retired` and
`test_goal_no_longer_feeds_the_appraisal` pin that so they cannot be quietly resurrected.
No migration was run and no stored historical result was recalculated.

**Appraisal authorization, aligned with the model:**

| Persona | Behaviour |
|---|---|
| Ordinary employee | own appraisal only (list and document) |
| Manager | read-only down the `reports_to` chain |
| HR User / HR Manager | broad **within their authorised companies** |
| Company-fenced HR | another company's appraisals are invisible and unreadable |
| `System Manager` | **no** HR-wide visibility (removed from `UNRESTRICTED_APPRAISAL_ROLES`) |
| `Administrator` | framework authority |
| No employee, no scope | `1=0` — fails closed |

`UNRESTRICTED_APPRAISAL_ROLES` now derives from `HR_SEE_ALL_ROLES`, and both
`get_permission_query_conditions` and `has_permission` compose HR access with
`hrms.overrides.company_scope` instead of returning blanket access.

**Product defects found and fixed while tracing the workflow:**

| Defect | Impact |
|---|---|
| `kra_category` never copied from template to Appraisal KRA | The column the fork deliberately unhid rendered empty on every appraisal |
| Score-conversion guard used `has_value_changed` on a **child table**, which reports a change on every save | `complete_cycle` was impossible for any cycle that had appraisals — i.e. every real one. Now compares row values |
| `System Manager` counted as HR for appraisal visibility | Confidential appraisals visible to a technical role |
| HR appraisal access ignored the company fence | HR could read other companies' appraisals |

**Test-fixture defects corrected** (no assertion weakened): HR fixtures carried an
`allow=Employee` User Permission that silently narrowed `get_list` regardless of role;
HR fixtures matched the cycle's designation filter and joined it as appraisees, skewing
counts; `create_appraisals` leaves the cycle doc stale, so completing it needed a reload.

### v16 defects fixed

| Finding | Fix | Verification |
|---|---|---|
| **RC-A** 29 doctypes gave staff level-0 rights on employee-keyed rows with no fence | `overrides/employee_owned_row_scope.py` on **both** hooks; own + DocShare + HR-in-company + Administrator; `1=0` fail-closed; `TEAM_REVIEWED_DOCTYPES` keeps the Attendance Request manager view working | **[S]** fence guard 4/4 · **[RT]** 12/12 personas |
| **RC-B** 12 Script Reports bypass row scope | 9 restricted to HR (final classification, patch for existing sites); **`Shift Attendance` and `Employee Advance Summary` keep staff access and now self-scope** via `utils/report_scope.py`; `Appraisal Overview` already self-scoped | **[S]** report integrity 2/2 · **[RT]** patch applied on install |
| **RC-C** System Manager inherited HR read scope | G1 | **[RT]** |
| **Symptom 1** employee cannot save a Leave draft | mandatory approver enforced at `docstatus == 1` | **[RT]** `test_leave_draft_creation` 7/7 |
| Approver-set mismatch (UI offered ancestors the fence refused) | `hr/utils.get_designated_approvers()` — single source of truth for selector and validator | **[RT]** |
| **Leave Ledger dual-writer** (R-3) | `write_block.block_transactions_for_mirrored_employee` on Leave Application `before_submit`/`before_cancel`; same escape hatches, logged break-glass | **[S]** hooks wiring 19/19 |
| `get_leave_balance_map` raised `UnboundLocalError` on every call | resolve-then-guard; 9 stale test call-sites adapted | **[RT]** 11/11 |
| `get_current_employee` raised `AttributeError` for a user with **no Employee mapping** | `(… or {})` → fails closed with `PermissionError` | **[RT]** |
| `employee_ctc_break_up.py` nested f-string quotes = **SyntaxError on py<3.12** | single quotes inside | **[S]** all 883 modules parse on 3.10 |
| **4 aggregate queries** using SQL functions as SELECT strings — rejected by Frappe v16 (`sum(claimed_hours)`, `sum(hours_cost)`, `min(time)`, `sum(amount) as total`) — broke the RL bank summary, the late-checkout probe and the interco report | rebuilt via row-sum / ordered-limit / query builder | **[RT]** surfaced *and* cleared by `test_requests_history_api` |
| **4 custom fields existed only in a patch** (`restrict_user_permission_to_hrms`, `requires_remote_approval`, `remote_approval_status`, `is_abandoned`). `install_app` marks patches pre-applied, so on **any fresh site** they were never created — the geofenced check-in flow, OT calculation, stale-IN sweeper and HRMS-only UP scope all failed with `Unknown column` | `utils/extension_custom_fields.py` merged into `setup.get_custom_fields()`, mirroring `get_provenance_custom_fields` | **[RT]** fresh site: 0 → **4/4 fields**; `test_ot_request` 12/12 |
| Test-fixture defects surfaced by a clean site: `test_leave_balance` created only `GRADE_D` (slabs need `GRADE_F`), `TestGetLeaveTypes` created no grade, an LWP allocation the framework refuses, my own leave-draft date overlaps | repaired | **[RT]** 11/11 |

### Final gate results

| Gate | Result |
|---|---|
| `ruff check hrms/` | **All checks passed** (was 4 errors, all pre-existing) |
| `python3 -m compileall hrms/` | **OK** |
| AST parse of all 883 modules at `feature_version=(3,10)` | **0 invalid** |
| `git diff --check` | **clean** |
| Static suites (fence, report-role, doctype-perm, write-block) | **27 tests OK** |
| `node --test frontend/tests/*.test.mjs` | **27/28** — the one failure is `call-error-handling.test.mjs`, which imports `frappe-ui` source and needs `frontend/node_modules` (not installed); unrelated |
| **Bench suites on `fresh.local`** | **165 tests OK** across 14 modules: team_status 11 · filing_window 7 · requests_history 4 · employee_owned_row_scope 12 · manager_leave_visibility 5 · team_api 6 · leave_draft_creation 7 · leave_balance 11 · ot_request 12 · staff_lockdown 17 · approval_row_scope 8 · write_block 19 · company_fence 41 · staff_approver_fence 5 |
| Fresh-install migration | `bench new-site` + `install-app hrms` clean; `restrict_staff_script_reports` recorded in Patch Log |

**One pre-existing failure recorded, not fixed:** `hrms.overrides.test_remote_checkin_request_hooks` (2 errors on a clean site — the employee sits under the root "All Departments" node, which has no `company`, so `Department.save()` fails; a second assertion expects a different approver priority). Neither the test nor its module was touched by this work; a one-line company fix triggers `NestedSetRecursionError`, so it needs its own fixture rework. **Recorded as debt, not migration-related.**

### ERP–HRMS contract — external boundary, still blocked

**[V] No ERP→HRMS role or scope synchronisation exists in either branch.** `hrms/sync/` mirrors four HR *data* doctypes and never roles or User Permissions; `api/erp_instance.py` is a PWA deep-link. The only automatic role grants are `Leave Approver`/`Expense Approver` from `overrides/employee_master.py:92-114`.

HRMS now enforces the intended model against its **effective local** roles and scopes: HR broad access composes with the company fence rather than bypassing it, `System Manager` carries no confidential HR sight, seniority grants nothing, managers get only `reports_to` scope inside their company, `Administrator` keeps framework authority, and every unresolved case fails closed. **End-to-end ERP provisioning and revocation remains unverifiable here and stays WP-10** — nothing was invented to fill it.

## 10d. Login / provisioning failure — root cause and fix (2026-08-14)

**Symptom.** Microsoft authentication completes, the browser lands on
`/hrms/invalid-employee` with "No active employee found associated with the email ID",
and two `401`s appear against `/api/method/login`. Reported for an HR login test.

### What actually failed

**Not one user's data — the absence of an identity layer.** The only `User -> Employee`
rule in the app was a raw `Employee.user_id == frappe.session.user` query, copy-pasted into
**13 call sites**, 7 of which spelled `status = "Active"` and 6 of which did not.

| # | Defect | Evidence | Effect |
|---|---|---|---|
| R1 | Nothing establishes the link for an SSO user | `frappe/utils/oauth.py::login_oauth_user` loads-or-creates a `User` from the ID-token email; no HRMS code reconciles it with an Employee | An ERP master that manages staff without portal users mirrors Employees with `user_id` empty → the person authenticates and is bounced forever |
| R2 | The mirror clobbers the link | `hrms/sync/runner.py::_mirror_payload` copied `user_id` verbatim from the source | Setting `user_id` by hand survived only until the next incremental pull. **This is precisely why a one-user database workaround was the wrong fix** — it silently un-fixed itself |
| R3 | The mirror desyncs `User.enabled` from `Employee.status` | `_write_row` updates via `frappe.db.set_value`, which fires no doc events, so ERPNext's `Employee.on_update -> update_user_status()` never runs | A Left employee keeps an *enabled* User (authenticates, then dead-ends); a re-activated one keeps a *disabled* User and cannot authenticate at all |
| R4 | Ambiguity resolved silently | `_write_row` inserts with `ignore_validate=True`, so `validate_duplicate_user_id` never runs; `frappe.db.get_value` then returns whichever row comes first | Two Active Employees can share a `user_id` and one person is handed the other's record |
| R5 | No diagnosis | One dialog string for five causes; nothing logged server-side | Every cause looked identical to the user and to support |

**The two `401`s were password attempts, not SSO.** `call("login", ...)` is reachable only
from `Login.vue`'s submit handler (`frontend/src/data/session.js:28,33` are its sole
callers; grep of `frontend/src` finds no other). An SSO-provisioned User is created with
`"new_password": frappe.generate_hash()`, so a password login by such a user can only ever
`401`. Microsoft authentication itself **did** create a valid Frappe session — the PWA
router only reaches `/invalid-employee` on the `isLoggedIn` branch
(`frontend/src/main.js:133`), so the redirect is proof the session existed.

### What was fixed, by ownership layer

**Canonical identity mapping — new `hrms/utils/identity.py`.** Normalizes the login
(`strip().lower()`); requires exactly one **Active** Employee claiming it; refuses ambiguity
rather than arbitrating it; distinguishes "no record" from "inactive record" from
"ambiguous". A single email fallback exists solely to *establish* the permanent link:
unique normalized match on `company_email`, Employee `user_id` still empty, written once and
audit-logged to Error Log. `personal_email` is deliberately excluded — `company_email` is
the employer-controlled address an IdP assertion corresponds to. No role is granted, no
domain is matched, no first-match is taken. All 13 call sites now route through it, so
inactive and ambiguous are denied everywhere, not only at login.

**Provisioning / sync — `hrms/sync/runner.py`.** `LOCALLY_OWNED_FIELDS` drops `user_id`
from every mirrored payload: the ERP stays authoritative for the identity *data*
(`company_email`, `status`, `company`) while this hub owns the login *mapping*.
`_reconcile_user_status()` restores the `User.enabled` <-> `Employee.status` invariant that
`db.set_value` skips.

**Diagnosis.** New whitelisted `hrms.api.get_employee_identity_status()` returns the reason
and a message that names no other employee, company or record; `InvalidEmployee.vue` shows
it. Every denial is logged server-side with the reason and, for ambiguity, the conflicting
employee IDs.

### Invariant deliberately NOT changed

**An HR user still needs an Employee record to enter the PWA.** The product evidence is
explicit — `frontend/src/main.js:137` ("user should be an employee to access the app since
all views are employee specific") — and no evidence of a contrary intent exists in either
branch. Desk access is unaffected. HR was not exempted from Employee validation, and no
blanket permission was added.

### Residual, for an HR decision — not fixed here

Sites mirrored **before** this change may hold Users still enabled whose Employee is
`Left`/`Inactive` (R3's historical residue). The mirror now corrects each such row the next
time it is pulled, but a row modified before the fix will not be re-pulled. Deliberately
**not** mass-disabled by a patch: bulk account changes need HR sign-off. Query to list them:

```sql
SELECT e.name, e.status, e.user_id FROM `tabEmployee` e
JOIN `tabUser` u ON u.name = e.user_id
WHERE e.status != 'Active' AND u.enabled = 1 AND e.synced_from_instance IS NOT NULL;
```

### Console messages, classified

| Message | Status |
|---|---|
| `apple-mobile-web-app-capable` deprecated | **Fixed** — `mobile-web-app-capable` added; the Apple tag stays, iOS Safari reads only that one |
| Leaflet from `unpkg.com` (CSS **and** JS) | **Fixed** — `leaflet@1.9.4` is now a package dependency, imported by `CheckInPanel.vue`; marker icons inline as data URIs, so the map has no third-party request and works offline. The 5-second poll that waited for the deferred CDN script is gone with it |
| `'beforeinstallprompt' event was fired.` | **Fixed** — removed; `install()` now null-guards the single-use captured event, and is still called only from the Install button's `@click` |
| `tile.openstreetmap.org` tile requests | **Expected** — a map needs a tile server; not a tracking dependency of the app shell |
| `fonts.googleapis.com` / `fonts.gstatic.com` | **Unrelated** to this fix and left alone — same third-party class as Leaflet was, and a candidate for the same treatment if the policy extends to webfonts |

## 10e. Leave approval never finalized — root cause and fix (2026-08-14)

**Complaint.** "When a Leave Application is approved, it is not finalized automatically.
It remains in a state that requires an HR Manager to submit or resubmit it manually."

### Reproduced on `fresh.local`, synthetic personas only

| Case | Payload the UI actually sent | docstatus | status | ledger |
|---|---|---|---|---|
| approve, named approver | `{status: Approved, docstatus: 1}` | 1 | Approved | 1 |
| **reject, named approver** | `{status: Rejected}` | **0** | Rejected | **0** |
| **approve, status only** | `{status: Approved}` | **0** | Approved | **0** |
| ...then the redundant HR step | `{docstatus: 1}` | 1 | Approved | 1 |
| unrelated approver | denied outright (`get_doc_permissions` → `{None: 0}`) |
| double coupled approve | idempotent; ledger stays 1 |

### Root cause

The decision-to-final-state coupling lived in the **frontend**, in
`RequestActionSheet.vue::updateDocumentStatus`:

```js
if (status === "Approved" && hasPermission("submit")) docstatus = 1
```

Three defects in one line:

1. **Asymmetric** — only `Approved` was coupled, so **every rejection** parked at
   `docstatus 0` and the sheet then rendered its separate Submit button.
2. **Client-decided** — the coupling depended on a client-side read of
   `frappe.client.get_doc_permissions`, which runs this fork's `approval_row_scope`
   `has_permission` hook per document. When it returned `submit: 0`, or had simply not
   loaded, the approval degraded to a half-transition with **no error**.
3. **Not centralized** — nothing server-side required a decided request to reach
   `docstatus 1`, so every other caller reproduced the same half-state.

### The intended state machine, derived from evidence

* `Leave Application.on_submit` — *"Only Leave Applications with status 'Approved' and
  'Rejected' can be submitted"*. `Shift Request` and `Expense Claim` state the identical
  rule in their own `on_submit`.
* `status` is `permlevel 1`, `reqd`, `no_copy`, and **not** `allow_on_submit` — so the
  decision cannot be made after submission; it must ride the same save. There is
  therefore **no legal resting state where a request is decided but still a draft**.
* **No Workflow exists** on Leave Application: `frappe.get_all("Workflow", ...)` is empty
  and `tabLeave Application` has no `workflow_state` column. Not a workflow-mapping defect.
* `Employee` and `Employee Self Service` hold no `submit` right, so **the draft *is* the
  pending-approval state** — confirmed by `on_update`, which notifies the approver exactly
  on `status == "Open" and docstatus < 1`.
* `Leave Approver` holds level-0 `submit` **and** permlevel-1 `write` in both DocPerm and
  Custom DocPerm, so the approver could always complete the transition. The frontend was
  simply not asking them to.

So: **draft+Open (pending) → one decision, which sets the status and submits in the same
save → docstatus 1 + Approved/Rejected (final) → cancel → docstatus 2 + Cancelled.**

### Entry points, and how each is treated

| Entry point | Treatment |
|---|---|
| PWA Approve / Reject (`RequestActionSheet`) — Leave Application, Shift Request, Expense Claim | **Fixed** — now calls `hrms.api.approval.decide` |
| PWA Submit / Cancel buttons (same sheet) | Unchanged; still the repair path for rows already stuck half-transitioned |
| PWA `FormView` status field + Submit | Unchanged **by decision** — an explicit two-step form edit, not a silent half-transition; `report_half_transitioned` catches anything left behind |
| Desk edit-then-submit | Unchanged — Frappe's native model; blocking it would be a regression |
| `WorkflowActionSheet` | Inert: no Workflow is defined on any of these doctypes |
| Direct `frappe.client.set_value` / REST | Still legal (Desk depends on it); the resulting half-state is now reportable |
| Email / notification actions | None exist for these doctypes on this branch |

### Fix

**New `hrms/api/approval.py::decide(doctype, name, status)`** — one whitelisted, POST-only
server-side transition for the three decide-then-submit doctypes. Takes a row lock,
re-checks state under it, sets the decision field and calls `doc.submit()`, so one
authorized action performs decision + finalization + ledger in **one Frappe save cycle and
one transaction**.

Permissions stay entirely with Frappe — `check_permission("write")`,
`check_permission("submit")`, permlevel enforcement on the decision field, the
`approval_row_scope` `has_permission` hook, and `before_submit` →
`block_transactions_for_mirrored_employee`. **No `ignore_permissions`, no `db_set`, no
flag manipulation.** The doctype list is an allow-list, not a caller-supplied fieldname,
so the endpoint cannot be turned into a write-anything.

Idempotent by construction: already decided the same way → no-op success; decided
differently, or cancelled → refused.

### Persona and lifecycle results — 27 tests, green on `fresh.local` and `test.local`

Draft is the pending state · one approval reaches the final state · **rejection now
reaches it too** · exactly one ledger entry (`leaves = -1.0`) · no second HR submission ·
repeated and triple approval stay at one ledger entry · reversing a decision refused ·
cancelled request cannot be decided · ordinary employee, unrelated approver and
System-Manager-without-HR all denied · HR User, HR Manager, Administrator permitted ·
cross-company HR denied · arbitrary doctype refused · only Approved/Rejected accepted ·
failing downstream step rolls back to draft+Open with no ledger · cancellation removes the
ledger effect · amendment starts undecided (`status` is `no_copy`) · **mirrored employee
refused** · half-transitioned report finds and repairs nothing.

### Existing inconsistent records — reported, not repaired

`hrms.api.approval.report_half_transitioned()` (HR User / HR Manager / System Manager
only) returns counts and rows for all three doctypes. Deliberately **not** auto-submitted:
submitting one writes Leave Ledger Entries and consumes the employee's balance, and
nothing in the row distinguishes a real decision that never landed from an abandoned
click. **HR decision required.** Equivalent SQL:

```sql
SELECT name, employee, company, status, modified, modified_by
FROM `tabLeave Application` WHERE docstatus = 0 AND status IN ('Approved','Rejected');
-- same shape for `tabShift Request`.status
-- and for `tabExpense Claim`.approval_status, where 'Draft' is the undecided value
```

### Regression evidence

Baseline was measured by stashing the change and re-running the identical 21-module sweep.
The only difference post-fix is `test_leave_approval_lifecycle` going from 0 tests (module
absent) to **27 passing**; every other module's failure set is byte-identical. Pre-existing
and untouched: `test_leave_application` 7 (`test_leave_approver_mandatory`,
`test_leave_approver_perms`, `test_overlap`, `test_overlap_with_half_day_1/2/3`,
`test_overlap_with_half_day_on_today`) and `test_leave_encashment` 1
(`test_status_of_leave_encashment_after_payment_via_salary_slip`).

## 11. Appendix — capability → commit SHAs (reverse traceability)

Abbreviated SHAs in the range `dd6c388f7..2fb61f399`.

- **C01-pms-appraisal (63):** 01cb9e05f 03bd2184c 06b569e7d 0a245c47c 10648431f 146364fba 19daa8fbb 2960c3a0e 3134acc9e 316640d9f 3182c3e79 3206294b3 3378f4fbc 3ae255196 3b15f2470 3c9c603d9 3d4b0a3d7 3df2f5a53 402bb4d51 444b97cc9 518fefbb5 555b20c02 57c15e82a 59c5ceec5 5ccbf0f5e 5e9bdfc9d 6872c423a 6cbaa4644 70b8b43ed 72859289e 744469eb6 75fd5ba75 79dfd20c0 8011b195a 8467ac65d 8553014fa 89a325404 90f6ff517 95ad89cc8 9a37c4b87 9b534845c 9dc1cb246 a1dc76a6e a304f0b68 a55e80847 a6a0a5021 a9d6ab137 ac24623d3 bda714494 c727ea30e d655ac66e dbca0aa6f df6a16f38 e41e9cb77 e4ef1b2d2 e8c06e8f3 e9b262e2b ec06eb993 f21539b8f f2452f3c6 f7ded749e f8062317d f8c8aff9e
- **C04-overtime (11):** 11e16f9ac 21c75e6e6 78277102a 8c7cc499c 9707d66ee 9b27c0736 9dbae39d5 bceffafac d042e3e4b d7e6e5375 daeac23ac
- **C05-geofence-checkin (27):** 042d5b5ed 1d6d8d740 306cdfeec 361fafdde 3a02a1a26 4ef4a0911 549568d57 6c22c68c9 6ea9dcf7c 70408a14e 727cff77b 74cce9a5c 8625f5cdc 880849632 8af3a3d3e 8d42dec7b 9a4902de5 9fe82d4e7 a127b5255 a483ee77d aa7b6e9d4 b091cc994 b73e78919 c546915b8 ea7f192b9 f6cc39100 fd67c2692
- **C06-sop-library (9):** 09dcb0f47 0e773cb99 2b3288b41 409c70667 5848afe24 a065907fe c900f0d24 cec38c10c f22c5a81d
- **C07-employee-issue (6):** 11c6c7aff 5b6b61eec 91973d1d2 ad4854bb5 bfe7b5905 e180fb2bd
- **C08-permissions-and-lockdown (24):** 07082e8f1 0d8708644 0e8f32f75 1c875dbbd 2b8207bea 36a4df831 44ee308f1 47052e5f9 5bfa3bd9e 6d30b2010 74b804d20 7be70c71d 8dddd232d 9aa2ef2bc 9cfb948ec 9da59688b b8a3ec4dc be451031d c537edb61 ef73a5dd4 f13e3f59c f6b9a586c fb87dc890 fc7f22da6
- **C09-employee-master (1):** 1ba28cc69
- **C10-shift-rules-and-breaks (7):** 4bc9bc4ad 4f4dbfd80 89b93b5b7 8b6d9954e 960269c42 e34da2442 e9c6f73fb
- **C12-leave-rules (17):** 075863175 143eb8c32 21050b3f3 34d4f1425 3c4adc075 419aa84db 4da6e170a 5896e7ebe 5aa3cd9cd 5bf14c706 6053a6334 96c4fb92f a6084dd0f c0b9b3be8 cc71e3014 e839dbdc3 fdceaf9fb
- **C16-attendance-request (4):** 0076451a1 83e97370b bac47bc83 ead62fd97
- **C17-regional-payroll (4):** 07ed3f56a 297a7fb80 4799989d9 4be1b749c
- **C18-interco-cost (3):** 13e01331e 75ae51663 d910ec2ca
- **C19-employment-hero (1):** 8619d2037
- **C20-skills-gap (2):** 94d5f6a29 cd1fef935
- **C21-pwa-ux (12):** 0955a187c 426b594c8 72f95bbc3 773bf7b14 7ce220e04 8c80d953c 9ac126aef 9ae95bce5 9f8e92f64 aa0b01e9f bcddb1659 eb2717a43
- **C23-pwa-notifications (6):** 312af58bc 3ad678b75 6bcc72438 cf4f5df63 f588f64ea f6924a306
- **C26-tooling-and-docs (1):** 0ba3e2653
- **G1 (1):** 87ae095e7 · **G2 (1):** acad140f8 · **G3 (1):** dbc63acd0 · **G4 (1):** 1cca17e28 · **G5 (1):** 4e33cad0d · **G6 (1):** 9a52eaa44 · **G7 (1):** 2614bd5ae
- **R00-release-version-bump (107):** all `chore(release)` commits; `2c73c1867` additionally carried release tooling (§2.2).

*Sum: 63+11+27+9+6+24+1+7+17+4+4+3+1+2+12+6+1 = 198 already satisfied; +7 gap = 205 substantive; +107 release-only = 312 total.*
