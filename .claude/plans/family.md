CLASS: an approved request's typed values dropped by a message when the day already had a row
hrms/hr/doctype/attendance_request/attendance_request.py create_or_update_attendance — same-root (fixed: apply_typed_hours writes in/out/hours; paid day refuses in words)
hrms/hr/doctype/attendance_request/attendance_request.py new-row branch — not-affected — already wrote the times
hrms/hr/doctype/leave_application/leave_application.py create_or_update_attendance — not-affected — leave carries no times
