CLASS: a rework that silently drops what the old screens did (review of 518a541e7).

Instances and verdicts:
frontend/src/views/Profile.vue DETAIL_FIELDS — same-root: grade and preferred_email restored (the old Company/Contact sheets showed them).
frontend/src/views/Profile.vue togglePush (off) — same-root: the confirmation the Settings page gave is back.
frontend/src/views/Profile.vue getFieldValue reports_to branch — same-root: dead since the manager moved onto the page; removed.
