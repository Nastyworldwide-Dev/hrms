CLASS: a sheet or first-load placeholder that breaks the iOS rules the pages now keep (loose line, hand-ruled list, wrong-size skeleton)
frontend/src/components/HolidayList.vue — same-root (fixed: rows in one group, empty answer a row)
frontend/src/components/LeaveBalance.vue + glass/GBalanceGrid.vue — same-root (fixed: one skeleton card, full width)
other 9 sheets — not-affected — sheet-consistency-audit: 0 findings
design/gates/ios.mjs + run.mjs — the lock-in: the four audits as one gate
