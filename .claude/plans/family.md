CLASS: one optional source failing a whole screen's data. get_month_flags
builds four dot kinds; the announcement read raised when the board was
missing and took leave, holiday and needs-you dots down with it (403).

Callers of announcements._visible_rows:
hrms/api/calendar.py:_event_days — same-root, fixed here (fails soft to no event dots, logged).
hrms/api/announcements.py list/home/mark-read (154, 171, 207, 270) — not-affected: they ARE the board; a missing board there is the board's own error, and the PWA silences home_announcements already (loudRequest SILENT_ENDPOINTS).
