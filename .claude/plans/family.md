CLASS: a long flat approvals list that read as a leak.

Call sites: hrms/api/approvals_list.py _row/_remote_row — same-root (section, department, approver_name added; gates unchanged). _mine_of yours_only — narrows only. needs_you Home count — yours only. NeedsYou.vue check-in row — reads needs_you.checkins. Approvals.vue — grouped. Profile.vue remote badge — not-affected (a badge for the check-in queue page, counts all you may act on). Access: unchanged (same _request_read_allowed + _is_routed_approver).
