CLASS: a Home block that vanished when empty, so Home read as broken.

Call sites: NeedsYou.vue (approvers) — same-root, fixed. Announcements.vue — same-root, fixed. NowBar — not-affected (already always shows a state). CheckInPanel — P0-9 ruled: check in as normal, button stays.
