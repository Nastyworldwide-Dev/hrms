CLASS: a page reserving the tab bar height that the shell already reserves.

Call sites: More.vue, TeamDashboard.vue, TeamRoster.vue, SopList.vue pb-24 — same-root, fixed (pb-4). Other tab pages — not-affected (no pb-24; grep clean).
