diff --git a/node_modules/frappe-ui/src/resources/documentResource.js b/node_modules/frappe-ui/src/resources/documentResource.js
index a93673e..7bc4761 100644
--- a/node_modules/frappe-ui/src/resources/documentResource.js
+++ b/node_modules/frappe-ui/src/resources/documentResource.js
@@ -41,18 +41,23 @@ export function createDocumentResource(options, vm) {
       }
     },
     beforeSubmit(params) {
-      out.previousDoc = JSON.stringify(out.doc)
-      Object.assign(out.doc, params.fieldname || {})
+      out.previousDoc = JSON.stringify(out.doc ?? null)
+      Object.assign(out.doc || {}, params.fieldname || {})
       // update data in list resources
       updateRowInListResource(out.doctype, out.doc)
     },
     onSuccess(data) {
       out.doc = transform(data)
-      out.originalDoc = JSON.parse(JSON.stringify(out.doc))
+      out.originalDoc = out.doc == null ? null : JSON.parse(JSON.stringify(out.doc))
       options.setValue?.onSuccess?.call(vm, data)
     },
     onError(error) {
-      out.doc = JSON.parse(out.previousDoc)
+      // never let the rollback throw — it would mask the real server error
+      try {
+        if (out.previousDoc) out.doc = JSON.parse(out.previousDoc)
+      } catch (e) {
+        console.warn('Could not revert doc after failed setValue', e)
+      }
       options.setValue?.onError?.call(vm, error)
       // revert data in list resource
       revertRowInListResource(out.doctype, out.doc)
@@ -80,7 +85,7 @@ export function createDocumentResource(options, vm) {
         onSuccess(data) {
           saveLocal(cacheKey, data)
           out.doc = transform(data)
-          out.originalDoc = JSON.parse(JSON.stringify(out.doc))
+          out.originalDoc = out.doc == null ? null : JSON.parse(JSON.stringify(out.doc))
           options.onSuccess?.call(vm, out.doc)
         },
         onError(error) {
diff --git a/node_modules/frappe-ui/src/resources/resources.js b/node_modules/frappe-ui/src/resources/resources.js
index 9196763..1a10339 100644
--- a/node_modules/frappe-ui/src/resources/resources.js
+++ b/node_modules/frappe-ui/src/resources/resources.js
@@ -154,7 +154,12 @@ export function createResource(options, vm) {
     out.error = error
     for (let fn of errorFunctions) {
       if (fn) {
-        fn.call(vm, error)
+        try {
+          fn.call(vm, error)
+        } catch (e) {
+          // a throwing error-callback must not replace the real error
+          console.warn('Error in onError handler', e)
+        }
       }
     }
     // if no error handler is defined, use fallbackErrorHandler
diff --git a/node_modules/frappe-ui/src/utils/call.js b/node_modules/frappe-ui/src/utils/call.js
index a1b84fa..b7ee23f 100644
--- a/node_modules/frappe-ui/src/utils/call.js
+++ b/node_modules/frappe-ui/src/utils/call.js
@@ -50,6 +50,10 @@ export default async function call(method, args, options = {}) {
       error = JSON.parse(response)
       // eslint-disable-next-line no-empty
     } catch (e) {}
+    if (!error || typeof error !== 'object') {
+      // non-JSON body (e.g. gateway error page) — keep a usable message
+      error = { _error_message: `Request failed with status ${res.status}` }
+    }
     let errorParts = [
       [method, error.exc_type, error._error_message].filter(Boolean).join(' '),
     ]
@@ -65,9 +69,14 @@ export default async function call(method, args, options = {}) {
     e.exc_type = error.exc_type
     e.exc = exception
     e.status = res.status
-    e.messages = error._server_messages
-      ? JSON.parse(error._server_messages)
-      : []
+    e.messages = []
+    if (error._server_messages) {
+      try {
+        e.messages = JSON.parse(error._server_messages)
+      } catch (err) {
+        console.warn('Could not parse _server_messages', err)
+      }
+    }
     e.messages = e.messages.concat(error.message)
     e.messages = e.messages.map((m) => {
       try {
