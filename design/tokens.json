{
	"color-constant": {
		"brand": {
			"value": "#C8FF00",
			"description": "Action fill, active indicators, progress fill"
		},
		"brand-2": {
			"value": "#A8DC00",
			"description": "Action gradient end"
		},
		"danger": {
			"value": "#F87171",
			"description": "Error. Fills and rules in both themes; text and border roles use danger-ink on light (spec 2.3)"
		},
		"leave": {
			"value": "#7B5CFF",
			"description": "On leave"
		},
		"media-frame": {
			"value": "#111318",
			"description": "Camera viewfinder frame: dark in both themes (a viewfinder is never a white slab)"
		},
		"neutral-dot": {
			"value": "#8A929E",
			"description": "Legend and decorative markers"
		},
		"on-brand": {
			"value": "#0A0C05",
			"description": "Text and icons on brand fill"
		},
		"on-media-frame": {
			"value": "#F5F6F8",
			"description": "Words on the media frame, both themes"
		},
		"rest": {
			"value": "#B6BDC9",
			"description": "Rest day, calendar legend"
		},
		"success": {
			"value": "#00E5C0",
			"description": "Resolved / approved"
		},
		"tile-announcement": {
			"value": "#FF4245",
			"description": "alpha.7 §7 icon tile: Apple iOS system colour for this kind (owner Q2)"
		},
		"tile-expense": {
			"value": "#0091FF",
			"description": "alpha.7 §7 icon tile: Apple iOS system colour for this kind (owner Q2)"
		},
		"tile-fix": {
			"value": "#40C8E0",
			"description": "alpha.7 §7 icon tile: Apple iOS system colour for this kind (owner Q2)"
		},
		"tile-leave": {
			"value": "#30D158",
			"description": "alpha.7 §7 icon tile: Apple iOS system colour for this kind (owner Q2)"
		},
		"tile-neutral": {
			"value": "#8E8E93",
			"description": "alpha.7 §7 icon tile: Apple iOS system colour for this kind (owner Q2)"
		},
		"tile-on-tile": {
			"value": "#FFFFFF",
			"description": "The white symbol on a coloured icon tile (iOS Settings)"
		},
		"tile-overtime": {
			"value": "#FF9230",
			"description": "alpha.7 §7 icon tile: Apple iOS system colour for this kind (owner Q2)"
		},
		"tile-shift": {
			"value": "#6D7CFF",
			"description": "alpha.7 §7 icon tile: Apple iOS system colour for this kind (owner Q2)"
		},
		"tile-sop": {
			"value": "#B78A66",
			"description": "alpha.7 §7 icon tile: Apple iOS system colour for this kind (owner Q2)"
		},
		"warn": {
			"value": "#F59E0B",
			"description": "Warning. Fills and rules in both themes; text and border roles use warn-ink on light (spec 2.3)"
		}
	},
	"color-themed": {
		"accent-glow": {
			"value": {
				"light": "rgba(63,92,0,.14)",
				"dark": "rgba(200,255,0,.45)"
			},
			"description": "Glow behind hero numerals"
		},
		"accent-ink": {
			"value": {
				"light": "#3F5C00",
				"dark": "#C8FF00"
			},
			"description": "Accent text only. Brand #C8FF00 must never set type on light (spec 2.4)"
		},
		"bg": {
			"value": {
				"light": "#F2F2F7",
				"dark": "#000000"
			},
			"description": "Page ground: iOS systemGroupedBackground (alpha.7, owner's iOS 26 screenshot: #000 dark)"
		},
		"glass-fill": {
			"value": {
				"light": "rgba(255,255,255,.56)",
				"dark": "rgba(255,255,255,.075)"
			},
			"description": "Panel fill. Light is deliberately more opaque than dark; do not correct (spec 6)"
		},
		"glass-fill-fallback": {
			"value": {
				"light": "#FFFFFF",
				"dark": "#1C1C1E"
			},
			"description": "Content cell fill: iOS secondarySystemGroupedBackground (alpha.7, measured #1C1C1E)"
		},
		"glass-rim": {
			"value": {
				"light": "rgba(255,255,255,.90)",
				"dark": "rgba(255,255,255,.14)"
			},
			"description": "Panel border, 1px"
		},
		"hair": {
			"value": {
				"light": "rgba(16,22,34,.08)",
				"dark": "rgba(255,255,255,.075)"
			},
			"description": "Row dividers"
		},
		"icon-bg": {
			"value": {
				"light": "rgba(16,22,34,.05)",
				"dark": "rgba(255,255,255,.10)"
			},
			"description": "Icon wells. NOT progress tracks — see track-solid (spec 6.3)"
		},
		"ink": {
			"value": {
				"light": "#0B0C10",
				"dark": "#FFFFFF"
			},
			"description": "Primary text, numbers, headings"
		},
		"ink-muted": {
			"value": {
				"light": "#636366",
				"dark": "#8E8E93"
			},
			"description": "Tertiary text: placeholders, ticket IDs, timestamps"
		},
		"ink2": {
			"value": {
				"light": "#545C68",
				"dark": "#A8AEB8"
			},
			"description": "Secondary text, labels, captions. Never apply opacity multipliers (spec 2.5)"
		},
		"ink3": {
			"value": {
				"light": "#878F9B",
				"dark": "#6B7280"
			},
			"description": "Non-text only: chevrons, dividers, disabled. Light value deliberately raised from #8A929E to clear 3:1. Never apply opacity multipliers (spec 2.5)"
		},
		"lift": {
			"value": {
				"light": "0 10px 30px rgba(20,26,40,.10)",
				"dark": "0 8px 26px rgba(0,0,0,.34)"
			},
			"description": "Panel drop shadow"
		},
		"rim-hi": {
			"value": {
				"light": "rgba(255,255,255,.95)",
				"dark": "rgba(255,255,255,.28)"
			},
			"description": "Inset highlight, top edge"
		},
		"rim-lo": {
			"value": {
				"light": "rgba(16,22,34,.06)",
				"dark": "rgba(0,0,0,.28)"
			},
			"description": "Inset shadow, bottom edge"
		},
		"scrim": {
			"value": {
				"light": "rgba(11,12,16,.32)",
				"dark": "rgba(0,0,0,.56)"
			},
			"description": "Modal and action-sheet backdrop (spec 10.3 #25, #26). Not in the spec's token table: CustomIonModal's focus-trap workaround hand-builds its backdrop, so a scrim value is required and the mockup never showed one"
		},
		"sheen": {
			"value": {
				"light": "rgba(255,255,255,.55)",
				"dark": "rgba(255,255,255,.13)"
			},
			"description": "Diagonal gloss overlay"
		},
		"sheet-bg": {
			"value": {
				"light": "#F2F2F7",
				"dark": "#1C1C1E"
			},
			"description": "A form sheet's ground: iOS systemGroupedBackground in a sheet (dark sheets are the elevated #1C1C1E). alpha.7 B21/B22"
		},
		"sheet-cell": {
			"value": {
				"light": "#FFFFFF",
				"dark": "#2C2C2E"
			},
			"description": "A group inside a sheet: iOS secondarySystemGroupedBackground, elevated (dark #2C2C2E). alpha.7 B21/B22"
		},
		"surface": {
			"value": {
				"light": "#FFFFFF",
				"dark": "#1C1C1E"
			},
			"description": "Card/control surface — the glass tone (a subtle lift from the page), NOT the #313133/#ECEDEF bar-track. Decoupled so cards stop inheriting the heavy track: on the track, muted text fell to 3.41:1 (dark) / 4.14:1 (light), both below WCAG AA; on this tone it is 4.71 / 4.73. bg-surface maps here; bars keep track-solid."
		},
		"track-solid": {
			"value": {
				"light": "#ECEDEF",
				"dark": "#313133"
			},
			"description": "Opaque track behind disputable numbers: KPI ring, KRA bars, leave balance bars (spec 6.3 — a number a person may dispute with their manager must not be read through a moving tint). Values are the exact composite of icon-bg over glass, so the appearance is unchanged; being solid, it no longer shifts with what sits behind it"
		}
	},
	"color-semantic": {
		"danger-ink": {
			"value": {
				"light": "#D70015",
				"dark": "#FF4245"
			},
			"description": "Status/error text: Apple iOS 26 system colour (dark); light is the accessible variant, >= 4.8:1 on #F2F2F7 and #FFFFFF (alpha.7)"
		},
		"warn-ink": {
			"value": {
				"light": "#B24A00",
				"dark": "#FF9230"
			},
			"description": "Status/error text: Apple iOS 26 system colour (dark); light is the accessible variant, >= 4.8:1 on #F2F2F7 and #FFFFFF (alpha.7)"
		},
		"warn-fill": {
			"value": {
				"light": "#8F3F05",
				"dark": "#D97706"
			},
			"description": "The offline bar FILL, carrying --g-bg as its text. Not --g-warn-ink: that is a TEXT colour for glass, and at #B45309 the 10.5px caption over it measures 4.36 against the 4.5 floor. DISTINCT from warn-ink in BOTH themes on purpose — identical values are two names for one colour, and the tokens gate is right that a swap between them would then be invisible. Light 6.33, dark 6.32."
		},
		"leave-ink": {
			"value": {
				"light": "#5D46C2",
				"dark": "#C4B5FF"
			},
			"description": "Leave text and border. Light measured 4.55 on the violet-26% tint"
		},
		"success-ink": {
			"value": {
				"light": "#1F7A36",
				"dark": "#30D158"
			},
			"description": "Status/error text: Apple iOS 26 system colour (dark); light is the accessible variant, >= 4.8:1 on #F2F2F7 and #FFFFFF (alpha.7)"
		}
	},
	"shadow": {
		"focus-ring": {
			"value": "0 0 0 2px var(--g-ink), 0 0 0 5px var(--g-brand)",
			"description": "Keyboard focus-visible ring (WCAG 2.4.7): 2px ink halo + 3px brand ring. Rides --g-ink so it inverts to white in dark. Single source — every :focus-visible references this, not a copied literal"
		},
		"focus-ring-inset": {
			"value": "inset 0 0 0 2px var(--g-ink), inset 0 0 0 5px var(--g-brand)",
			"description": "Inset variant of focus-ring for controls inside an overflow:hidden panel (list rows), where the outer ring would be clipped at the panel edge. Same ink+brand language, drawn inward so it stays fully visible"
		}
	},
	"spacing": {
		"screen-gutter": {
			"value": "16px",
			"description": "Left/right padding of all screen content Moved to the 4pt grid, 22 Sep 2026 (was 15px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"stack-sm": {
			"value": "8px",
			"description": "Between sibling cards in a grid Moved to the 4pt grid, 22 Sep 2026 (was 9px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"stack-md": {
			"value": "12px",
			"description": "Between major blocks Moved to the 4pt grid, 22 Sep 2026 (was 13px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"stack-lg": {
			"value": "16px",
			"description": "Below the screen title"
		},
		"pad-panel": {
			"value": "16px",
			"description": "Balance cards, stat tiles Moved to the 4pt grid, 22 Sep 2026 (was 14px 13px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"pad-row": {
			"value": "12px 16px",
			"description": "List rows Moved to the 4pt grid, 22 Sep 2026 (was 11.5px 15px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"pad-action": {
			"value": "16px 20px",
			"description": "Primary and ghost buttons Moved to the 4pt grid, 22 Sep 2026 (was 17px 18px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"pad-badge": {
			"value": "4px 8px",
			"description": "Badges Moved to the 4pt grid, 22 Sep 2026 (was 3.5px 8px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"sheet-pad-bottom": {
			"value": "16px",
			"description": "Bottom padding inside a sheet, ADDED to the safe-area inset. Single-value on purpose: pad-panel is two-value ('14px 13px'), so max(var(--g-pad-panel), env(...)) was invalid CSS, the declaration was dropped, and every sheet's primary button sat flush against the viewport floor Moved to the 4pt grid, 22 Sep 2026 (was 14px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"fab-bottom": {
			"value": "calc(var(--g-tabbar-height) + var(--g-tabbar-gap) + var(--g-tabbar-pad-top) + var(--g-tabbar-pad-bottom))",
			"description": "Bottom offset of a floating action button on a tab destination. DERIVED from the bar's own tokens rather than measured: SopList carried bottom-[76px], which was correct against a 64+9 bar and silently wrong the moment any of the four values moved. Ionic forces content-box on the tab bar host, so the paddings sit OUTSIDE tabbar-height and all four terms are needed."
		}
	},
	"radius": {
		"radius-action": {
			"value": "20px",
			"description": "Buttons, map panel, selfie panel Moved to the 4pt grid, 22 Sep 2026 (was 19px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"radius-banner": {
			"value": "16px",
			"description": "Banners, issue cards"
		},
		"radius-card": {
			"value": "16px",
			"description": "Balance cards, goals panel Moved to the 4pt grid, 22 Sep 2026 (was 17px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"radius-group": {
			"value": "26px",
			"description": "iOS 26 inset-grouped list corner, measured on the owner's iPhone Settings screenshot (alpha.7 §5.3, review A11)"
		},
		"radius-input": {
			"value": "12px",
			"description": "Form fields Moved to the 4pt grid, 22 Sep 2026 (was 14px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"radius-panel": {
			"value": "20px",
			"description": "Glass panels, list containers"
		},
		"radius-pill": {
			"value": "8px",
			"description": "Badges, pills Moved to the 4pt grid, 22 Sep 2026 (was 6px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"radius-tabbar": {
			"value": "32px",
			"description": "Tab bar capsule: half its 62 pt drawn height, rounded up to the grid (HIG: a capsule's radius is half its height). alpha.7 §5.1."
		},
		"radius-tile": {
			"value": "16px",
			"description": "Stat tiles Moved to the 4pt grid, 22 Sep 2026 (was 15px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"radius-well": {
			"value": "8px",
			"description": "Icon wells Moved to the 4pt grid, 22 Sep 2026 (was 9px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		}
	},
	"blur": {
		"blur-panel": {
			"value": "20px",
			"description": "All glass panels"
		},
		"blur-ghost": {
			"value": "18px",
			"description": "The ghost action only"
		}
	},
	"type": {
		"family": {
			"display": {
				"value": "-apple-system, BlinkMacSystemFont, 'Inter Tight', 'Inter', 'Segoe UI', Roboto, sans-serif",
				"description": "Display stack. Do not bundle SF Pro"
			},
			"ui": {
				"value": "-apple-system, BlinkMacSystemFont, 'Inter', 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif",
				"description": "UI stack"
			},
			"mono": {
				"value": "'SF Mono', 'JetBrains Mono', ui-monospace, 'Cascadia Mono', monospace",
				"description": "Mono stack. In-app, not documentation-only"
			}
		},
		"scale": {
			"clock": {
				"family": "display",
				"size": "34px",
				"weight": 700,
				"tracking": "-0.01em",
				"line-height": 1.0,
				"description": "Check-in time Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 36px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"display-number": {
				"family": "display",
				"size": "34px",
				"weight": 700,
				"tracking": "-0.01em",
				"line-height": 1.0,
				"description": "Balance number, e.g. 7.5 Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 31px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"ring-centre": {
				"family": "display",
				"size": "28px",
				"weight": 700,
				"tracking": "-0.01em",
				"line-height": 1.0,
				"description": "KPI ring centre, e.g. 4.2 Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 25px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"stat-number": {
				"family": "display",
				"size": "22px",
				"weight": 700,
				"tracking": "-0.01em",
				"line-height": 1.0,
				"description": "Stat number, e.g. 10 PRESENT Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 22px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"screen-title": {
				"family": "display",
				"size": "20px",
				"weight": 600,
				"tracking": "-0.01em",
				"line-height": 1.15,
				"description": "Screen title, e.g. My KPI Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 21.5px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"button-label": {
				"family": "display",
				"size": "17px",
				"weight": 600,
				"tracking": "-0.01em",
				"line-height": 1.2,
				"description": "Button label, e.g. CHECK IN Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 15.5px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"panel-title": {
				"family": "display",
				"size": "17px",
				"weight": 600,
				"tracking": "-0.01em",
				"line-height": 1.2,
				"description": "Panel title, e.g. August 2026 Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 14.5px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"row-label": {
				"family": "ui",
				"size": "15px",
				"weight": 400,
				"tracking": "0",
				"line-height": 1.4,
				"description": "Row label, e.g. Apply for leave Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 12.5px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"card-title": {
				"family": "ui",
				"size": "15px",
				"weight": 600,
				"tracking": "0",
				"line-height": 1.4,
				"description": "Card title Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 12.5px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"kra-label": {
				"family": "ui",
				"size": "15px",
				"weight": 600,
				"tracking": "0",
				"line-height": 1.4,
				"description": "KRA label, e.g. Data accuracy Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 11.5px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"eyebrow": {
				"family": "ui",
				"size": "13px",
				"weight": 400,
				"tracking": "0",
				"line-height": 1.3,
				"description": "Eyebrow, e.g. H1 2026 REVIEW. No opacity multipliers (spec 2.5) Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 10.5px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"caption": {
				"family": "ui",
				"size": "12px",
				"weight": 400,
				"tracking": "0",
				"line-height": 1.45,
				"description": "Caption, e.g. Last check-out 6:17 pm Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 10.5px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"field-label": {
				"family": "ui",
				"size": "13px",
				"weight": 400,
				"tracking": "0",
				"line-height": 1.3,
				"description": "Field label, e.g. DATE WORKED Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 10px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"micro-label": {
				"family": "ui",
				"size": "12px",
				"weight": 400,
				"tracking": "0",
				"line-height": 1.3,
				"description": "Micro label, e.g. ANNUAL LEAVE Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 10px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"badge": {
				"family": "ui",
				"size": "12px",
				"weight": 600,
				"tracking": "0",
				"line-height": 1.2,
				"description": "Badge, e.g. OPEN Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 10px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			},
			"tab-label": {
				"family": "ui",
				"size": "11px",
				"weight": 600,
				"tracking": "0",
				"line-height": 1.2,
				"description": "Tab bar label, e.g. HOME. 10px at 0.02em tracking, NOT a step on the 12-14-16-19-23-28-34-40 ramp and deliberately outside it. Five uppercase labels must fit one bar at the 320px floor, which gives each 57.6px: CALENDAR measures 50.7px here and 65.7px at the ramp's 12px step with its 0.07em tracking. That step shipped on 22 Sep 2026 and rendered CALENDARREQUESTS on every screen in the app, because each label's overflow:hidden clipped its own box while the boxes overlapped. The ramp governs type a person READS; a tab label is a fitting problem and the bar is the constraint that wins. Arithmetic in frontend/src/components/__tests__/tabbar-fits.test.js."
			},
			"data-system": {
				"family": "mono",
				"size": "12px",
				"weight": 400,
				"tracking": "0",
				"line-height": 1.5,
				"description": "Data / system: coordinates, ticket IDs Step on the 12-14-16-19-23-28-34-40 ramp (ratio ~1.2), set 22 Sep 2026; was 10px. Half-pixel sizes render at a different weight on every device pixel ratio, and steps closer than 1.125 are two names for one size."
			}
		}
	},
	"motion": {
		"button-press": {
			"duration": "120ms",
			"easing": "cubic-bezier(.2,.8,.3,1)",
			"property": "transform, box-shadow",
			"description": "Button press"
		},
		"row-tap": {
			"duration": "90ms",
			"easing": "ease-out",
			"property": "background",
			"description": "Row tap: background transitions to icon-bg"
		},
		"skeleton-shimmer": {
			"duration": "1400ms",
			"easing": "linear",
			"iteration": "infinite",
			"property": "transform: translateX",
			"description": "Skeleton shimmer. Becomes a static fill under prefers-reduced-motion"
		},
		"state-change": {
			"duration": "200ms",
			"easing": "cubic-bezier(.2,0,0,1)",
			"property": "background-color, color, border-color",
			"description": "A control changing state in place — a toggle, a chip, a selected row. Material 3's SHORT band (50-200ms): long enough to be seen as a change rather than a jump, short enough that a rapid tapper never waits. Replaces --motion-glide, which SopFormSheet referenced four times and which has been defined nowhere since the Modernist stylesheet was removed: four dead transitions that simply snapped."
		}
	},
	"layer": {
		"overlay": {
			"value": 100,
			"description": "Dialogs and action sheets. NOTE: sticky is numerically above this, so a sticky header currently paints over an open dialog. Preserved as-is in phase 3 — the values are promoted, not re-ordered; re-ordering is a behaviour change and needs its own review"
		},
		"sticky": {
			"value": 1000,
			"description": "Sticky screen headers"
		},
		"scrim": {
			"value": 10000,
			"description": "The hand-built modal backdrop (GModal), which must sit above everything including a sticky header"
		},
		"scrim-backdrop": {
			"value": 10000,
			"description": "The dimmer behind a hand-built modal. Same level as `scrim` by design: the backdrop and the sheet it dims are one surface in two elements, and z-[10000] was written by hand in CustomIonModal where nothing tied it to the token."
		}
	},
	"layout": {
		"content-column": {
			"value": "calc(100% - 32px)",
			"description": "Content column width. Was authored as '100% - 30px', which is not a valid CSS value on its own; wrapped in calc() so the layout group can be emitted Moved to the 4pt grid, 22 Sep 2026 (was calc(100% - 30px)): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"content-column-lg": {
			"value": "720px",
			"description": "Desktop content column at lg: (spec 20.3). SIGNED OFF at 720px on 22 September 2026. The spec offered it for tuning once somebody had seen it on a real screen; the owner accepted it unchanged. Written here rather than only in a plan, because a token that reads as unfinished invites the question to be re-opened by whoever meets it next."
		},
		"content-column-read": {
			"value": "720px",
			"description": "Long-form reading column (SOP detail). Held SEPARATELY from content-column-lg although both are 720px today: a reading measure is set by characters per line (Bringhurst: 45-75; 720px of 16px type is ~72) and an app column by layout, so a future change to one must not silently move the other. SopDetail drew 820px, which runs a line past 80 characters."
		},
		"group-gap": {
			"value": "35px",
			"description": "Gap between iOS inset groups, measured (alpha.7 §5.3)"
		},
		"row-height-icon": {
			"value": "54px",
			"description": "iOS 26 list row that carries an icon tile, measured; a plain row is 44 (touch-target-min). alpha.7 §5.3"
		},
		"sheet-max-height": {
			"fallback": "calc(100vh - 5rem)",
			"value": "calc(100dvh - 5rem)",
			"description": "Bottom-sheet ceiling (spec 10.3 #25). GModal owns this shape; the token exists so the CSS layer and the Tailwind max-h-sheet utility read the same value. dvh, not vh: 100vh is the viewport with the address bar RETRACTED, and the bar is showing most of the time, so a sheet sized against it runs its confirm button off the bottom of the screen. The vh fallback stays because a browser that cannot read dvh drops that declaration entirely."
		},
		"tabbar-border": {
			"value": "1px",
			"description": "Border width of the floating tab bar, one edge. Content-box adds it twice to the rendered height (top edge + bottom edge); the scroll reservation must count both."
		},
		"tabbar-gap": {
			"value": "8px",
			"description": "Gap beneath the tab bar, above the safe area. The tab bar never scrolls Moved to the 4pt grid, 22 Sep 2026 (was 9px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"tabbar-height": {
			"value": "52px",
			"description": "Height of one tab (the lens pill fills it). Drawn bar = border + pad-top + this + pad-bottom + border = 62 pt, matching iOS 26's ~61 measured (alpha.7 §5.1). 52 leaves 44+ for the target."
		},
		"tabbar-inset": {
			"value": "20px",
			"description": "Inset of the floating tab bar from the screen's left and right edges: ~20 pt, measured on the owner's iPhone (iOS 26; not published by Apple). alpha.7 §5.1, review A5."
		},
		"tabbar-pad-bottom": {
			"value": "4px",
			"description": "Bottom inner padding of the floating tab bar. Same content-box reasoning as tabbar-pad-top: it adds to the bar's rendered height and must be readable by the scroll reservation, not just by the bar's own rule. Moved to the 4pt grid, 22 Sep 2026 (was 9px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"tabbar-pad-top": {
			"value": "4px",
			"description": "Top inner padding of the floating tab bar. Ionic forces box-sizing: content-box !important on the ion-tab-bar host (light DOM cannot override it), so this padding sits OUTSIDE tabbar-height in the rendered box. Named so the scroll reservation (8.3) can read it instead of copying it — see tabbar-border. Moved to the 4pt grid, 22 Sep 2026 (was 11px): every screen density divides by 4, so an off-grid value lands on a fractional device pixel and rounds differently per handset."
		},
		"touch-target-min": {
			"value": "44px",
			"description": "Minimum touch target. Tappable area padded out to 44px without moving the visual"
		},
		"viewport-floor": {
			"value": "320px",
			"description": "The narrowest supported viewport (revamp §27). WCAG 2.1 SC 1.4.10 (Reflow) requires content to work at 320 CSS px without two-dimensional scrolling, and it is also where a 390px phone lands at 200% text zoom (SC 1.4.4). No layout in this app had ever been checked there until 22 Sep 2026; frontend/tests/responsive-floor.test.mjs now refuses a fixed width, a min-width or a fixed grid track that cannot fit inside it."
		},
		"viewport-height": {
			"value": "844px",
			"description": "Reference viewport height, CSS px"
		},
		"viewport-width": {
			"value": "390px",
			"description": "Reference viewport width, CSS px"
		}
	},
	"icon": {
		"icon-sm": {
			"value": "16px",
			"description": "The spec's own icon grid (§9): stroke line icons are drawn 16x16. Inline glyphs inside a row of text."
		},
		"icon-md": {
			"value": "20px",
			"description": "Default UI icon: list-row leading icons, chevrons, tab bar. Replaces the hand-picked 17, 18 and 19px that meant this."
		},
		"icon-lg": {
			"value": "24px",
			"description": "An icon that is the subject of its row rather than a marker beside it — empty-state glyphs, section headers. Replaces 22px."
		},
		"icon-xl": {
			"value": "32px",
			"description": "Avatar and large empty-state art. Replaces 30px and 34px."
		},
		"control-md": {
			"value": "44px",
			"description": "A square tappable control at the platform floor (HIG 44pt / Material 48dp). Anything smaller needs padding out, not shrinking."
		},
		"control-lg": {
			"value": "56px",
			"description": "Floating action button. Material 3's FAB is 56dp; the app drew 52, three px under a size every Android user recognises by touch."
		}
	}
}
